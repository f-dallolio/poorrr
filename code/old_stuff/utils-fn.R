# fn_env <- function(fn) {
#   environment(fn)
# }
#
# fn_env_name <- function(fn) {
#   e <- environment(fn)
#   if (isNamespace(e)) {
#     ns_env_name(e)
#   } else {
#     env_name(e)
#   }
# }
# environmentName(environment(\(x)x))
# environmentName(baseenv())
#
#
# functi
